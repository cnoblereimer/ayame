name: pangolin
services:
  # This node's half of the inter-node link. network_mode: host so the
  # wg-cluster interface lands in the HOST namespace and this node's
  # containers can route to ayame's 10.88.0.1 for Postgres and Redis.
  wireguard:
    image: lscr.io/linuxserver/wireguard:latest
    container_name: wireguard
    restart: unless-stopped
    network_mode: host
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    # No sysctls block here: docker refuses namespaced sysctls under
    # network_mode: host (it would be setting the host's, not the
    # container's). wg-quick only needs src_valid_mark for fwmark-based
    # policy routing, which a point-to-point link like this does not use.
    # If it is ever needed, set it on the host instead:
    #   sysctl -w net.ipv4.conf.all.src_valid_mark=1
    volumes:
      - ./wireguard:/config
      - /lib/modules:/lib/modules:ro # only needed if wireguard isn't built into the kernel
    healthcheck:
      test: ["CMD-SHELL", "wg show wg-cluster >/dev/null 2>&1"]
      interval: "10s"
      timeout: "5s"
      retries: 12
      start_period: "15s"

  # This node's own load balancer, mirroring ayame's. Both node IPs are in
  # DNS for every hostname, so clients spread across the two and neither
  # node is a single point of ingress - and each one health-checks both
  # backends, so it can still serve when the other is broken or draining.
  haproxy:
    image: haproxy:3.4-alpine
    container_name: haproxy
    restart: unless-stopped
    depends_on:
      - gerbil
    volumes:
      - ./haproxy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
      # Must exist and be owned by uid 99 before this starts, or haproxy
      # cannot bind its runtime socket and crash-loops:
      #   mkdir -p haproxy/run && sudo chown 99:99 haproxy/run
      - ./haproxy/run:/var/run/haproxy
    ports:
      - "80:80"
      - "443:443"
      - "3000:3000" # pangolin dashboard
      - "8404:8404" # haproxy stats page

  pangolin:
    image: docker.io/fosrl/pangolin:ee-postgresql-latest
    container_name: pangolin
    restart: unless-stopped
    environment:
      # Must match ayame's REDIS_PASSWORD - see michi/.env.example.
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    depends_on:
      # Its database and Redis are both across the tunnel, so starting
      # before it is up just means a crash loop until it comes up.
      wireguard:
        condition: service_healthy
    volumes:
      - ./config:/app/config
      - ./config/certificates:/var/certificates
      - ./config/dynamic:/var/dynamic
    ports:
      # Published directly on pangolin, not gerbil: gerbil's own port 53
      # publish does not correctly relay queries to pangolin's embedded
      # DNS server (confirmed on ayame by querying pangolin:53 directly
      # over the internal docker network, which answers correctly, vs.
      # the host's public port 53 via gerbil, which returned SERVFAIL).
      - "53:53/udp"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/api/v1/"]
      interval: "10s"
      timeout: "10s"
      retries: 15

  gerbil:
    image: docker.io/fosrl/gerbil:latest
    container_name: gerbil
    restart: unless-stopped
    depends_on:
      pangolin:
        condition: service_healthy
    command:
      - --reachableAt=http://<MICHI_PUBLIC_IP>:3004
      - --generateAndSaveKeyTo=/var/config/key
      - --remoteConfig=http://pangolin:3001/api/v1/
      - --trusted-upstreams=<AYAME_PUBLIC_IP>,<MICHI_PUBLIC_IP>
    volumes:
      - ./config/:/var/config
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    ports:
      - 51820:51820/udp # wireguard
      - 21820:21820/udp # relay
      # DNS (53/udp) is published on the pangolin service instead - see
      # its ports comment.
      - 3004:3004 # gerbil control API - needs to be reachable from ayame
      # 80/443/3000 are no longer published publicly: this node runs its own
      # haproxy now, and that owns those ports on the host. Its haproxy
      # reaches this traefik over the internal "pangolin" docker network.
      #
      # These tunnel-only publishes are for AYAME's haproxy, which must
      # reach this node's traefik DIRECTLY - pointing it at michi's public
      # 443 would hand traffic to michi's haproxy, which can send it back to
      # ayame, looping between the two load balancers.
      - 10.88.0.2:8080:80
      - 10.88.0.2:8444:8443
      - 10.88.0.2:8300:3000

  traefik:
    image: docker.io/traefik:v3.7.11
    container_name: traefik
    restart: unless-stopped
    network_mode: service:gerbil # ports appear on the gerbil service
    depends_on:
      pangolin:
        condition: service_healthy
    command:
      - --configFile=/etc/traefik/traefik_config.yml
    volumes:
      - ./config/traefik:/etc/traefik:ro
      - ./config/traefik/logs:/var/log/traefik
      - ./config/certificates:/var/certificates:ro
      - ./config/dynamic:/var/dynamic:ro
      # Separate from /var/certificates on purpose: Pangolin's own cert
      # janitor (cleanupUnusedCertificates in TraefikConfigManager.ts)
      # force-deletes any directory under certificates_path that isn't one
      # of THIS node's own currently-active domains, with only a ~15s grace
      # period. Michi never has an active domain of its own, so anything
      # synced into /var/certificates would get deleted moments after
      # landing there. This directory is never scanned by that logic.
      - ./cert-sync/synced-certs:/var/dashboard-cert:ro

networks:
  default:
    driver: bridge
    name: pangolin
