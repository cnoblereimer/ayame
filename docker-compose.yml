name: pangolin
services:
  postgres:
    image: postgres:17
    container_name: postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432" # published publicly so michi can reach it - lock this down with a firewall rule (see README)
    restart: always

  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379" # published publicly so michi can reach it - lock this down with a firewall rule (see README)
    restart: always

  haproxy:
    image: haproxy:2.9-alpine
    container_name: haproxy
    restart: unless-stopped
    depends_on:
      - gerbil
    volumes:
      - ./haproxy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    ports:
      - "80:80"
      - "443:443"
      - "3000:3000" # pangolin dashboard, load balanced across both nodes
      - "8404:8404" # haproxy stats page

  pangolin:
    image: docker.io/fosrl/pangolin:ee-postgresql-latest
    container_name: pangolin
    restart: unless-stopped
    depends_on:
      - postgres
      - redis
    volumes:
      - ./config:/app/config
      - ./config/certificates:/var/certificates
      - ./config/dynamic:/var/dynamic
    ports:
      # TEMPORARY: bound to loopback only, for initial setup via SSH tunnel.
      # Host-side ports shifted to avoid colliding with haproxy, which
      # already publishes 3000 on all interfaces (incl. 127.0.0.1).
      # Remove once a real domain/resource is configured and reachable
      # through the normal HAProxy -> gerbil -> traefik path.
      - "127.0.0.1:13000:3000"
      - "127.0.0.1:13002:3002"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/api/v1/"]
      interval: "10s"
      timeout: "10s"
      retries: 15

  gerbil:
    image: docker.io/fosrl/gerbil:latest
    container_name: gerbil
    restart: unless-stopped
    depends_on:
      pangolin:
        condition: service_healthy
    command:
      - --reachableAt=http://<AYAME_PUBLIC_IP>:3004
      - --generateAndSaveKeyTo=/var/config/key
      - --remoteConfig=http://pangolin:3001/api/v1/
      - --trusted-upstreams=<AYAME_PUBLIC_IP>,<MICHI_PUBLIC_IP>
    volumes:
      - ./config/:/var/config
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    ports:
      - 51820:51820/udp # wireguard
      - 21820:21820/udp # relay
      - 53:53/udp # DNS
      - 3004:3004 # gerbil control API - needs to be reachable from michi
      # 80/443/3000 are intentionally NOT published here: haproxy reaches
      # this node over the internal "pangolin" docker network instead
      # (gerbil:80, gerbil:8443, gerbil:3000), since haproxy already owns
      # those ports on the host.

  traefik:
    image: docker.io/traefik:v3.7.11
    container_name: traefik
    restart: unless-stopped
    network_mode: service:gerbil # ports appear on the gerbil service
    depends_on:
      pangolin:
        condition: service_healthy
    command:
      - --configFile=/etc/traefik/traefik_config.yml
    volumes:
      - ./config/traefik:/etc/traefik:ro
      - ./config/traefik/logs:/var/log/traefik
      - ./config/certificates:/var/certificates:ro
      - ./config/dynamic:/var/dynamic:ro

networks:
  default:
    driver: bridge
    name: pangolin

volumes:
  postgres_data:
